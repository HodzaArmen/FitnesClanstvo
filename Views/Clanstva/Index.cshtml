@model IEnumerable<FitnesClanstvo.Models.Clanstvo>

@{
    ViewData["Title"] = "Index";
}

<h1>Članstva</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tip)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Zacetek)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Konec)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tip)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Zacetek)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Konec)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Clan.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
